# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local-only render/inspection output
scratch/

# Build artifact written by scripts/prerender.mjs for the output verifier
prerender-report.json

# Graphify local generated output
graphify-out/

# Supabase CLI local-stack artifacts (`supabase start`). Machine-local state,
# never part of the repository.
supabase/.temp/
supabase/.branches/

# wrangler pages dev local asset cache
.wrangler/

# Local git worktrees created for background agent sessions.
.claude/worktrees/

# Logical production backup bundle produced by the Supabase production migration
# workflow. Database dumps are NEVER committed; they exist only as a run artifact.
production-backup/
